<h2 class = "page-header">Edit post</h2>
<%= render partial: 'form'%>
<div class="btn-container">
    <%= link_to 'Back', post_path(@post), class: 'back-btn'%>
    <%= button_to "Delete", @post, method: :delete,data: { confirm: "Are you sure?" }, class: 'delete-btn' %>
</div>